{% extends 'base.html.twig' %}

{% block title %}Pointage{% endblock %}

{% block body %}
    <h1>Pointage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pointage.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ pointage.date ? pointage.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ pointage.duree }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pointage_index') }}">back to list</a>

    <a href="{{ path('app_pointage_edit', {'id': pointage.id}) }}">edit</a>

    {{ include('pointage/_delete_form.html.twig') }}
{% endblock %}
